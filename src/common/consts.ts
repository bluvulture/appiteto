const PET_STATUSES = { available: 'available', sold: 'sold', pending: 'pending' };

const API_ENDPOINTS = { 
    petFindByStatus: "/pet/findByStatus",
    pet: "/pet"
};

export { PET_STATUSES, API_ENDPOINTS };
